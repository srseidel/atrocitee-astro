---
// Mark as server-rendered (required for authentication)
export const prerender = false;

// Redirect to the correct URL
export function getServerSideProps({ redirect }: any) {
  return {
    redirect: {
      destination: "/account/setting",
      permanent: true,
    },
  };
}
---

<meta http-equiv="refresh" content="0;url=/account/setting">
<script>
  window.location.href = "/account/setting";
</script> 